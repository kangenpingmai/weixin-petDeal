<view class="index-wraper">
  <view>
    <map id="map" longitude="{{longitude}}" latitude="{{latitude}}"
     scale="14" show-location
     controls="{{controls}}" 
     bindcontroltap="controltap" 
    ></map>
  </view>
  
  <view>
    <view bindtap="publishTO">
      发布
    </view>
    <view>
      搜索
    </view>
  <view wx:if="{{isIphonex}}"></view>
  </view>
</view>
